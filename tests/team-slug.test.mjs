import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import vm from 'node:vm';import ts from 'typescript';const ctx={exports:{},URL};vm.runInNewContext(ts.transpileModule(readFileSync('src/lib/team-slug.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,ctx);const {normalizeTeamSlug,memberSlug}=ctx.exports;
test('team slugs normalize profile URLs and reject invalid path segments',()=>{assert.equal(normalizeTeamSlug('https://mistravora.com/about/team/Shakeel-Mohamed'),'shakeel-mohamed');assert.equal(normalizeTeamSlug('bad/path'),null);assert.equal(normalizeTeamSlug('has spaces'),null);assert.equal(memberSlug({name:'Shakeel Mohamed',slug:null}),'shakeel-mohamed');assert.equal(memberSlug({name:'සිංහල',id:'member-id'}),'member-id');});
